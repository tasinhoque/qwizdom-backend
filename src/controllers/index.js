const authController = require('./auth');
const userController = require('./user');
const quizController = require('./quiz');

module.exports = {
  authController,
  userController,
  quizController,
};
